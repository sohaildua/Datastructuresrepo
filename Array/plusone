from typing import List

class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
         s = [str(i) for i in digits]
         res = int("".join(s)) + 1
         
         print(res)
         val = [int(x) for x in str(res)]
         return val

        
        
        
        

plusOne1 = Solution()

digits = [9,9]
print(plusOne1.plusOne(digits))